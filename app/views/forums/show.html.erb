<h1><%= @forum.name %></h1>
<%= link_to 'New Post', new_forum_post_path(@forum) %>
<ol>
  <% @posts.each do |post| %>
    <li><%= link_to post.title, forum_post_path(@forum, post) %></li>
  <% end %>
</ol>

<%= will_paginate @posts %>
