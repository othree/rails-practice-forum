<%= form_for @post, :url => forum_posts_path, :html => {:multipart => true} do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <%= f.file_field :file %>
  <%= f.submit "Create" %>
<% end %>
