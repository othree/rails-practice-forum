<h1><%= @post.title %></h1>
<p><%= @post.content %></p>
<p>
  <%= link_to 'edit post', edit_forum_post_path(@post.forum_id, @post) %>,
  <%= link_to 'delete post', forum_post_path(@post.forum_id, @post), :method => :delete %>
</p>
<p><%= link_to 'back to forum', forum_path(@forum) %></p>
